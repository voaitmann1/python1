#import TableGUIGrid
from TableGUIGrid import *
#window.mainloop()
#tv=TableView(frame_b, tk,        TableSize(len(dataSource),len(dataSource[0])), headerSize, ActiveCell, Align_Left1Top2Bottom3Right4,   DfltWidth, rowOrder_LC0_CL1, dataSource, sourceOrder_LC0_CL1)
#window.mainloop()
def ShowWindow(wnd):
    wnd.mainloop()

ShowWindow(window)
